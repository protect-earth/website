---
import Layout from '../../layouts/Layout.astro';
import ContentHero from '../../components/ContentHero.astro';
import TakeAction from '../../components/TakeAction.astro';
import { getCollection } from 'astro:content';
import TeamMember from '../../components/TeamMember.astro';
import { heroTeam } from '../../assets/hero-images';

const [memberCollection] = await getCollection('members');
const members = memberCollection.data ?? [];
---

<Layout title="Meet the Team">
	<!-- Hero Section with Background Image -->
	<ContentHero
		imgSrc={heroTeam}
		imgAlt="The whole team got together to visit Eden Projects in Cornwall, UK, with the biodomes in the background."
		title="Meet The Team"
		inferSize=true
	/>

	<!-- Team Members -->
	<div class="page-sections wrap">
		<div class="team-members">
			{members.map((member) => <TeamMember {...member} />)}
		</div>
	</div>

	<TakeAction />
</Layout>

<style>
	.team-members {
		display: flex;
		flex-flow: column;
		gap: var(--space-2xl);
	}
</style>
