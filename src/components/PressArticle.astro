---
import type { CollectionEntry } from 'astro:content';
type Props = CollectionEntry<'press'>['data'][0];

const { title, source, url, image, imageAlt, description, date } = Astro.props;
---

<article class="press-article">
	<img class="squircle" src={image} alt={imageAlt} />

	<div class="PressArticle__content">
		<header>
			<h2>{title}</h2>
			<dl>
				<dt class="visually-hidden">Source</dt>
				<dd>{source}</dd>

				<dt class="visually-hidden">Posted</dt>
				<dd>
					<time datetime={date.toISOString()}>
						{date.toLocaleDateString('en-GB', { month: 'short', day: 'numeric', year: 'numeric' })}
					</time>
				</dd>
			</dl>
		</header>

		<p class="prose">{description}</p>

		<a class="button" href={url} target="_blank" rel="noopener"> Read article </a>
	</div>
</article>
