---
type Props = {
	href: string;
	title: string;
	description: string;
};

const { href, title, description } = Astro.props;
---

<div class="section-card">
	<a href={href}>
		<h2>{title}</h2>
		<p>{description}</p>
	</a>
</div>
